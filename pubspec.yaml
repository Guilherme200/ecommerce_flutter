name: ecommerce_flutter
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: 0.5.2
  cloud_firestore: 0.14.3
  firebase_auth: 0.18.3
  provider: 4.3.2+2
  carousel_slider: ^2.3.1
  flare_flutter: ^2.0.6
  brasil_fields: ^0.6.0
  http: ^0.12.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/logos/
